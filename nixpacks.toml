[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
  "cd py-api",
  "pip install -r requirements.txt"
]

[start]
cmd = "cd py-api && uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT"
